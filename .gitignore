# Hard invariant #3 in CLAUDE.md. A copy of the taxi-drift source lives here
# so it can never enter this repo's history. See _to_ignore/WHY.md.
/_to_ignore

# Planning docs are tracked privately in AnmolS1/deadhead-private, via a second
# git-dir over this same work tree. See the `dhdocs` alias.
/docs
/CLAUDE.md

# Agent scratch
/.remember

# Dependencies & build output
node_modules/
dist/
dist-app/
.wrangler/
*.tsbuildinfo

# Local env
.env
.env.*
!.env.example
.dev.vars

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# S-02 purity-gate probes. Written and removed by scripts/validate/sim-purity.sh;
# listed here so an interrupted run can never leave one committed.
packages/sim/src/__purity_probe_*.ts

# Playwright MCP scratch output from browser-driven verification.
.playwright-mcp/
