node_modules
dist
dist-app
*.tsbuildinfo
package-lock.json

# Hard invariant #3 in CLAUDE.md — never read, never format.
_to_ignore

# Privately tracked; not this repo's business to reformat.
docs
CLAUDE.md

.remember

# Recorded golden data. These files are generated deliberately and compared
# byte-for-byte; a formatter must never rewrite them. See the S-14 note in
# TASKS.md on never editing a golden to make a test pass.
**/test/fixtures/
**/test/golden/

# Generated from packages/server/wrangler.jsonc by `wrangler types`, and kept
# honest by `npm run types:check` in the gate. Formatting it would make every
# regeneration a diff against the formatter rather than against the config.
packages/server/src/worker-configuration.d.ts
