/**
 * @deadhead/loadtest — headless bot clients.
 *
 * Scaffold only (D-01). The bots arrive in M-14 and drive plausible inputs
 * through `runLog()` from S-13 rather than inventing their own driver.
 */
import { SIM_VERSION } from '@deadhead/sim';

export const LOADTEST_VERSION = 0;

export { SIM_VERSION };
