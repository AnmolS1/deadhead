{
  "name": "@deadhead/simrun",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless replay CLI. Runs an input log and prints its score and hash trail.",
  "bin": {
    "simrun": "./dist/cli.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@deadhead/proto": "*",
    "@deadhead/sim": "*"
  }
}
