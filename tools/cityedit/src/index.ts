/**
 * @deadhead/cityedit — in-browser city authoring tool.
 *
 * Scaffold only (D-01). The editor arrives in W-02 and reuses the game
 * renderer rather than growing its own.
 */
import { PROTO_VERSION } from '@deadhead/proto';

export const CITYEDIT_VERSION = 0;

export { PROTO_VERSION };
