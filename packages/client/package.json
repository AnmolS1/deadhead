{
  "name": "@deadhead/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canvas renderer, input, audio and UI. Imports the sim; never mutates it.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run",
    "dev": "vite",
    "build:app": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@deadhead/proto": "*",
    "@deadhead/sim": "*"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
