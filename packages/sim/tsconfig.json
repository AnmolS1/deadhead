{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No "DOM". window/document/performance/fetch must not even typecheck here.
    // See CLAUDE.md hard invariant #1.
    "lib": ["ES2022"],
    "rootDir": "src",
    "outDir": "dist",
    "tsBuildInfoFile": "dist/.tsbuildinfo"
  },
  "include": ["src/**/*.ts"],
  "references": [{ "path": "../proto" }]
}
