{
  "name": "@deadhead/sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The deterministic simulation core. Pure ESM, zero runtime dependencies, no ambient globals.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@deadhead/proto": "*"
  }
}
